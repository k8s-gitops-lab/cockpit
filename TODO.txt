TODO — Initiative extensibilité / généricité
=============================================
Suivi détaillé : docs/backlog-extensibilite.md
Chaque axe s'implémente dans son repo propriétaire (jamais depuis control-plane),
commit + push sur les deux remotes (origin puis gitlab).

[x] Axe 1 — Schéma d'inventaire versionné + validation CI     (platform-gitops)  FAIT 2026-07-08

Phase 1 — fondations
[ ] Axe 2 — Contrat de variables plateforme (dé-hardcoder domaine/registre)
            Repos : platform-gitops, gitlab-projects-iac, ci-templates, infrastructure
            - Trou confirmé (2026-07-08) : gitlab_group_variable DOMAIN absente
              dans gitlab-projects-iac — deploy-gitops (ci-templates) lit déjà
              ${DOMAIN} mais rien ne la fournit en pipeline réel. Suivre le
              pattern for_each=local.app_groups déjà en place (app_ghcr_token).
            - Dédupliquer _PLATFORM_DEFAULTS/platform_constants() entre
              platform-cicd et toolbox (seule cette partie est identique)
            - 2b (plus lourd) : câbler le CR Flux terraform-gitlab.yaml
              (spec.vars.gitlab_url, en dur) sur apps.yaml.platform.domain,
              paramétrer les hostnames d'ingress des manifests plateforme

Phase 2 — refactor CI
[x] Axe 4 — ci-templates → composants CI versionnés (spec:inputs)   FAIT (détecté 2026-07-08 — implémenté précédemment, non documenté)
            Repo : ci-templates — templates/{build-kaniko,deploy-gitops,promote}/template.yml,
            consommés par helloworld/.gitlab-ci.yml via include:component@v2.0.0
[ ] Axe 5 — Séquence d'environnements déclarée par app
            Repos : platform-gitops + ci-templates
            - déclarer la séquence par noms (ex. environments: [dev, staging, prod])
            - consommer des deux côtés : rendu ArgoCD ET génération des jobs de deploy
              (ci-templates/templates/deploy-gitops/template.yml — chemin mis à jour)
            - preprod cesse d'être un cas spécial (juste un env de la liste)

Phase 3 — spikes (précédés d'une validation)
[ ] Axe 3 — Générateur natif ArgoCD (réduire render-argocd-apps.py)
            Repos : platform-cicd, platform-gitops
            - git files generator sur argocd/apps/*.yaml + goTemplate
            - trancher : ExternalSecrets/namespaces/projets natif vs scripté
[ ] Axe 7 — Multi-tenancy GitLab (token de projet par app)
            Repos : gitlab-projects-iac, platform-cicd
            - remplacer le PAT root partagé par un project access token scopé par app
            - plumbing de secrets CI

Différé
[ ] Axe 6 — Scaffolding d'app (app-template + toolbox)          (reporté 2026-07-08)

Divers / dette relevée en passant
[ ] Repousser tous les commits en attente vers le remote gitlab (injoignable ce jour)
[ ] Faire dériver platform_inventory._normalize_app du JSON Schema (anti-dérive)
[ ] Provisionner gitlab_group_variable DOMAIN par groupe d'app (gitlab-projects-iac)
    — gap fonctionnel confirmé, deploy-gitops la consomme déjà mais rien ne la
    fournit en pipeline GitLab CI réelle
